# generated data artifacts — rebuilt from data/pipeline
data/out/
# fetched book text, rebuilt from data/pipeline/excerpts.py — see BOOK_CATALOG
data/cache/

# rust
target/
**/*.rs.bk

# node
node_modules/
dist/
.vite/
# local copy of the sealed site artifact retained by the root release gate
apps/site/release-artifact/

# android
.gradle/
build/
local.properties

# os
.DS_Store
Thumbs.db

# python (content/scripts, data/pipeline)
__pycache__/
*.pyc

# scratch cache for data/pipeline/tests/measure_informativeness.py — the
# sample and the key are committed for reproducibility, this is not
data/pipeline/tests/.informativeness_hidden.json
# same convention for draw_usage_coverage_sample.py's blind draw
data/pipeline/tests/.usage_coverage_hidden.json
# generated wasm-bindgen output (crates/superb-wasm) — rebuilt by the wasm CI job
crates/superb-wasm/pkg/
