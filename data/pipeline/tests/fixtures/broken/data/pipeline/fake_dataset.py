# A stand-in for a pipeline script that reads a third-party dataset. It is
# deliberately not mentioned anywhere in the sibling MANIFEST.md's "Used
# for" column, so data/pipeline/tests/test_check_license_gate.py can prove
# check_license_gate.py catches a dataset with no manifest row.
