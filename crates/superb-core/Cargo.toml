[package]
name = "superb-core"
version = "0.0.0"
description = "The Superb engine: word state machine, scheduler, ability estimator, composer. Pure — no clock, no RNG, no I/O."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
syn = { workspace = true }
